# Dependencies for CV System
# ==========================

# YAML parsing
pyyaml>=6.0

# Template rendering (optional, for more complex templates)
jinja2>=3.1

# PDF parsing (for CV and offer ingestion)
pdfplumber>=0.9.0

# PDF generation (optional, for non-Pandoc approaches)
# weasyprint>=59.0  # Uncomment if using WeasyPrint instead of Pandoc
